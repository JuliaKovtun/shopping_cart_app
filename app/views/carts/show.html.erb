<div class="max-w-5xl container mx-auto px-4 py-8">
  <div class="flex justify-between items-center mb-6">
    <h2 class="text-3xl font-semibold text-gray-900">Cart</h2>
    <%= button_to 'Reset', reset_carts_path, method: :post, class: "text-sm text-gray-500 hover:text-red-600 underline" %>
  </div>

  <div id="cart-items">
    <%= render partial: 'cart_items/cart_items', locals: { cart_items: @cart_items } %>
  </div>
</div>
